@model IEnumerable<ProdajaMeda.Areas.Administrator.Models.ViewModels.Prodavnica.OrdersForAdminVM>

@{
    ViewBag.Title = "Orders";
}

<h2>Porudžbine</h2>


@if (!Model.Any())
{
    <h3 class="text-center">There are no orders.</h3>
}
else
{
    <table class="table">
        <tr>
            <th>
                Order Number
            </th>
            <th>
                Username
            </th>
            <th>
                Order Details
            </th>

            <th>
                Created At
            </th>
            <th>
                Ukupno
            </th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.OrderNumber)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Username)
                </td>
                <td>
                    @foreach (var item2 in item.ProductsAndQty)
                    {
                        @item2.Key @:x @item2.Value <br />
                    }
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CreatedAt)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Total)&nbsp;RSD
                </td>
            </tr>
        }

    </table>
}